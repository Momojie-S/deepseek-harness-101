# 依赖
node_modules/

# 编译产物
lib/
dist/
*.tsbuildinfo

# 个人配置（agent 个人指令，不提交；AGENTS.md 是项目级指令，要提交）
.agents/
AGENTS.local.md

# 系统
.DS_Store
Thumbs.db
.debug/

# dsh 运行时产物（version-check 状态、watchtest 等；项目配置 mcp.servers.yml 豁免，已跟踪文件不受影响）
.dsh/*
!.dsh/mcp.servers.yml

# dsh workspace-env（本地凭据，绝不入库）
.env
